PORT=4000
MONGODB_URI=mongodb://localhost:27017/lms_billing
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Used to encrypt stored SMTP app passwords (Configure Email). Use a long random string in production.
ENCRYPTION_KEY=gqdt atxd qzfe dmlo


# Optional: restrict CORS to your frontend URL (e.g. http://localhost:5173). Omit or set to "true" to allow all.
# FRONTEND_ORIGIN=http://localhost:5173
